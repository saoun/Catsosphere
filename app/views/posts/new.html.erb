<%= render "navbar" %>

<div class="container post">
<%= link_to "&larr; Back".html_safe, posts_path %>
  <% if current_user %>
    <h3>New Post</h3>
    <%= render "form" %>
  <%else%>
    <h3><%= link_to "Sign up", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %> to create a new post!</h3>
  <%end%>
</div>
